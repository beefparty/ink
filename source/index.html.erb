---
pageable: true
per_page: 10
---
<div id="container" class="js-packery" data-packery-options='{ "itemSelector": ".blog", "gutter": 30 }'>

<div class="blog head">
<h1>HELLO</h1>
</div>

<% if paginate && num_pages > 1 %><div class="blog">
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %></div>
<% end %>

<% page_articles.each_with_index do |article, i| %>
  <div class="blog"><%= image_tag article.data['header_image'], :width => '100%', :class => 'header_image' %>
  <h2><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span></h2>
  <!-- use article.summary(250) if you have Nokogiri available to show just
       the first 250 characters -->
  <%= article.summary(250) %></div>
<% end %>

<div class="blog"><%= lorem.sentences 5 %></div>

<% if paginate %><div class="blog">
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %></div>
<% end %>

</div>